<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= link_to "Go back to the list", tasks_path %>
</p>

<%= link_to "Delete #{@task.title.to_s}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<p><%= link_to "Edit the task", edit_task_path(@task) %>
    </p>
